interface:
  display_name: "Cursor Subagents"
  short_description: "Delegate coding tasks to Cursor CLI from Codex"
  default_prompt: "Use $cursor-subagents to delegate a bounded task to Cursor CLI and verify its results."
